<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.crafts.platform.mapper.UserMapper">

    <resultMap id="UserResultMap" type="com.crafts.platform.entity.User">
        <id property="id" column="id"/>
        <result property="username" column="username"/>
        <result property="password" column="password"/>
        <result property="realName" column="real_name"/>
        <result property="phone" column="phone"/>
        <result property="email" column="email"/>
        <result property="avatar" column="avatar"/>
        <result property="role" column="role"/>
        <result property="status" column="status"/>
        <result property="createTime" column="create_time"/>
        <result property="updateTime" column="update_time"/>
    </resultMap>

    <select id="findByUsername" resultMap="UserResultMap">
        SELECT id, username, password, real_name, phone, email, avatar, role, status, create_time, update_time
        FROM `user`
        WHERE username = #{username}
        LIMIT 1
    </select>

    <insert id="insert" parameterType="com.crafts.platform.entity.User" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO `user` (username, password, phone, role, status)
        VALUES (#{username}, #{password}, #{phone}, #{role}, #{status})
    </insert>
</mapper>
